.gradle/
build/
run/
*.log

# Hand-off copies staged at the repo root. Both duplicate something already
# tracked: the zip bundles the jar with a copy of this source tree, and the
# manual's real home is TeslesSeasons/docs/.
TeslesSeasons-*.zip
/TeslesSeasons-Manual.pdf
